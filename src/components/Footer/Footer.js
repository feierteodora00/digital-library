import React from 'react';

/**
 * A Footer for the bottom of each page.
 * 
 * A footer has been hardcoded to be easily placed at the bottom of evey page. It 
 * is successfully displayed at the bottom of all webpages on the API.
 * 
 * @author Jake Ellerington
 */
class Footer extends React.Component {

    render() {
    return (
        <div className="footer">
            <p>&copy; Teasm 27</p>
            
        </div>
    );
    }
}

export default Footer;